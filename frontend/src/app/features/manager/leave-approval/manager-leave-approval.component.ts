import { Component, OnInit } from '@angular/core';
import { CommonModule, DatePipe } from '@angular/common';
import { FormsModule } from '@angular/forms';
import { HttpClient, HttpHeaders, HttpParams } from '@angular/common/http';
import { AuthService } from '../../../core/services/auth.service';
import { SharedSidebarComponent } from '../../shared/shared-sidebar/shared-sidebar.component';
@Component({ selector: 'app-manager-leave-approval', standalone: true, imports: [CommonModule, FormsModule, DatePipe, SharedSidebarComponent], templateUrl: './manager-leave-approval.component.html', styleUrls: ['./manager-leave-approval.component.scss'] })
export class ManagerLeaveApprovalComponent implements OnInit { requests: any[] = []; notes: Record<number, string> = {}; search = ''; status = 'Pending'; error = ''; constructor(private http: HttpClient, private auth: AuthService) {} ngOnInit(): void { this.load(); } load(): void { let params = new HttpParams(); if (this.search) params = params.set('search', this.search); if (this.status) params = params.set('status', this.status); this.http.get<any[]>('http://localhost:8080/api/v1/manager/leaves', { params, headers: this.headers() }).subscribe({ next: data => this.requests = data || [], error: e => this.error = e.error?.error || 'Gagal memuat pengajuan tim' }); } decide(id: number, status: 'Approved' | 'Rejected'): void { this.http.put(`http://localhost:8080/api/v1/manager/leaves/${id}/approve`, { status, notes: this.notes[id] || '' }, { headers: this.headers() }).subscribe({ next: () => this.load(), error: e => this.error = e.error?.error || 'Gagal memproses pengajuan' }); } private headers(): HttpHeaders { return new HttpHeaders().set('Authorization', `Bearer ${this.auth.getToken()}`); } }
